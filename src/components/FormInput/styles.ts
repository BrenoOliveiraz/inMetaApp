import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({

  container: {
    marginBottom: 18
  },

  label: {
    marginBottom: 6,
    fontSize: 14,
    fontWeight: "600",
    color: "#333"
  },

  input: {
    backgroundColor: "#F7F7F7",
    borderRadius: 8,
    padding: 14,
    fontSize: 15,
    borderWidth: 1,
    borderColor: "#eee"
  }

})