import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({

  button: {
    backgroundColor: "#1976D2",
    padding: 16,
    borderRadius: 10,
    alignItems: "center",
    marginTop: 10
  },

  text: {
    color: "#fff",
    fontSize: 16,
    fontWeight: "600"
  }

})